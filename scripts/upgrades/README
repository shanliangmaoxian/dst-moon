
## 动机 & 作用
此文件夹用于处理持久化数据的升级程序，允许自动用新版本程序加载旧数据。

**升级程序错误将可能导致数据发生永久性的改变，并且无法再次通过升级程序得到预期数据，甚至无法发现那些是损坏的数据，因为这个原因没办法将这些损坏数据排除在升级程序之外，所以请务必检查它的正确性**

## 作用范围
支持以下作用域：
- 组件级 （未实现）
- 多值组件：多值组件中每个 Key 的 version 都是独立的; 已实现自动版本号，调用者不再需要手动指定版本号。如果改变了某个 Key 的数据结构，需要在 scripts/upgrades/[组件名].lua 中导出的 multivalue_upgrades 中指定对应 Key 的升级程序，并且永远只能在末尾添加同时不删除以往的升级程序才能保证完整升级。

## 名词解释
### 多值组件是什么？
在本 Mod 中，多值组件是说一个实体中的某个组件能被用于多种独立的用途，例如饥荒原版中的 Counter 组件可以通过 Key 来区分各个独立的 Counter 实例数据，这样做的好处是当你擦除或者清理数据时，仅移除你指定的 Key 对应的部分数据，而不需要移除整个组件。在一些 Mod 中经常见到有的 Mod 作者在清理逻辑中移除组件的做法，这非常容易导致其它 Mod 甚至是原版逻辑中的假设失效，进而引发无法预测的问题。